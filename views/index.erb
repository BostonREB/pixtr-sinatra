<div id="introduction">Below are a list of galleries that contain images of a variety of things.</div>

<h2>Galleries</h2>
<a href="/galleries/new">Create A New Gallery</a>
<div id="galleries">
  <ul>
    <% @galleries.each do |gallery| %> <!-- iterates over the gallery array -->
    <!-- |gallery| is a bound value.  Each time it is run, a new gallery is bound to that variable.  Variable value changes based on what the block does to it -->
    <li>
      <a href='/galleries/<%= gallery.id %>'>
        <%= gallery.name %>
      </a>
    </li>
    <% end %>
  </ul>
</div>

